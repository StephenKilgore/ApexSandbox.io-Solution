public Set<Integer> setUnion(Set<Integer> set1, Set<Integer> set2) {
    Set<Integer> resultSet = new Set<Integer>();
    for (Integer i : set1) {
        resultSet.add(i);
    }
    for (Integer i : set2)
    {
        resultSet.add(i);
    }
    return resultSet;
}