public Set<Integer> setIntersection(Set<Integer> set1, Set<Integer> set2) {
    List<Integer> resultSet = new List<Integer>();
    for (Integer s1 : set1)
    {
        for (Integer s2 : set2)
        {
            if (s1 == s2) resultSet.add(s1);
        }
    }
}