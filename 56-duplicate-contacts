public Boolean duplicateContacts(Contact c1, Contact c2) {
    return (String.isNotEmpty(c1.Phone) && String.isNotEmpty(c2.Phone) && c1.Phone == c2.Phone) || 
    (String.isNotEmpty(c1.Email) && String.isNotEmpty(c2.Email) && c1.Email == c2.Email);
}