public Integer maxConsecutiveOnes(Integer[] numbers) {
    String intString = '';

    for (Integer num : numbers)
    {
        intString += String.valueOf(num);
    }
    return Pattern.matches('1*', intString).length();
    
}