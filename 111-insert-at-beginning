public void insertAtStart(String cityName, List<String> cities) {
    {
        cities.add(cityName);
        for (Integer i = cities.size()-1; i >= 0; i--)
        {
            cities.add(cities[i]);
            cities.remove(i);
        }
    }